-- let
--   withDefault =
--     \(value : Text) ->
--     \(r : {name : Text, among : List Text, def : Optional Text}) ->
--       {name = r.name, among = r.among, def = [value] : Optional Text}
-- in let
--   attr =
--     \(name : Text) ->
--     \(among : List Text) -> 
--       { name = name
--       , among = among
--       , def = [] : Optional Text
--       }
-- in
  { name = "Event"
  
    -- Possible types
  , typ =
      { among = ["State", "Process", "Transition"]
      , def = [] : Optional Text
      }
  
    -- Attributes
  , attributes =
      [ { name = "Class"
        , among = ["Occurrence"]
        , def = [] : Optional Text
        }
      ]
  }

{ among : List Text
-- If `def` not provided while `required == True`, the first value from the list
-- is used as the default one.
, def : Optional Text
-- If not `required, then the value of the attribute does not have to be
-- specified.
, required : Bool }

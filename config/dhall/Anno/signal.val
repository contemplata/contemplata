let
  Attr = ./Attr.typ
in
  { name = "Signal"
  
    -- Possible types
  , typ =
      { among = ["Locative", "Measure", "Boundary", "Orientation"]
      , def = ["Locative"] : Optional Text
      }
  
    -- Attributes
  , attributes = [] : List {name : Text, value : Attr}

    -- Type-dependent attributes
  , attributesOnType = [] : List
      { typ : Text
      , attributes : List {name : Text, value : ./Attr.typ} }
  } : ./Entity.typ

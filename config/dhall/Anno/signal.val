let
  Attr = ./Attr.typ
in
  { name = "Signal"
  
    -- Possible types
  , typ =
      { among = ["Locative", "Measure", "Boundary", "Orientation"]
      , def = ["Locative"] : Optional Text
      }
  
    -- Attributes
  , attributes = [] : List {_1 : Text, _2 : Attr}

    -- Type-dependent attributes
  , attributesOnType = [] : List
      { key : Text
      , val : List {_1 : Text, _2 : Attr} }
  } : ./Entity.typ
